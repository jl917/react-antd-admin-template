import React from 'react';
import { Layout } from 'antd';
import s from '@/common/layout/style.styl';

const { Footer } = Layout;

const GlobalFooter = () => <Footer className={s.footer}>sad3f2</Footer>;

export default GlobalFooter;
